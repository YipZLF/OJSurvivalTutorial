8
asdb asd
hellow owrld!
    while(n--){
        getline(cin,str);
        for(int i = 0;i<str.length();i++){
            if(('a'<= str[i] && str[i]<'z')||
                ('A'<= str[i] && str[i]<'Z')){
